# 2026-08-23 — 15a813b3

## Shipped
- **Code sha:** 15a813b3 (PR #356)
- **Schema version:** 20260831000001 (unchanged)
- **Migrations applied:** none
- **Config / env changed:** none
- **Risk dimensions touched:** long-running / elapsed-time (worker loop). No
  schema, no config — the safest category by the table in README.md.

## Why
Production's attachment loop was crashing with

    OverflowError: int too large to convert to float
      email_ingestion.py:942 in idle_backoff

roughly every 8.5 hours of an idle queue (`2 ** (n-1)` exceeds float range at
n≈1025 at a 30s ceiling). Last observed 2026-08-23T02:13:11Z. The fix clamps the
exponent; no returned value changes.

## Pre-deploy evidence
- **Verification run id:** 20260823T044041Z
- **Prod lanes:** backend pass (166.1s, executed), migration-order pass
  (cached), frontend pass (cached), schema-compat **red — cannot run** without
  `SUPABASE_ACCESS_TOKEN` and a linked project. No migrations in this change,
  so that lane gates nothing here; it must be cleared before the next migration.
- **Regression proof:** reverting the fix makes both new tests fail with the
  production error. The pre-existing test stopped at `idle_backoff(100)`, where
  `2**99` still converts cleanly, so it could never have caught this.
- **Previous production deploy:** b9b7ae02, live since 2026-08-22T15:57Z.

## Observation

Production is served at **https://api.selkoapp.com** (the `.onrender.com` host
404s; the primary domain is only visible in the deploy logs). Recording that
here because two separate checks tonight were run against the wrong host --
`selko.onrender.com` is *staging*, on the free plan, and its degraded state was
briefly mistaken for production's.

| Window | /health | ingestion | egress | dead letters | task restarts |
|---|---|---|---|---|---|
| T+0 (06:21Z) | degraded | 4/4 tasks alive | — | 0 / 0 | 0 |
| T+15m (06:28Z) | degraded | 4/4 tasks alive | 449.8 MB counted, ~5 MB/h | 0 / 0 | 0 |
| T+24h | | | | | |

Tasks at both windows: `email-sync-coordinator`, `email-acquisition`,
`email-attachment`, `email-sync-health-floor` — all alive, 0 restarts, no
exception codes. `email-attachment` is the loop that was crashing. The pg
listener is connected with 0 reconnects. `open_incidents` went 1 -> 0 and
`integrations_due` 1 -> 0.

### Two anomalies recorded rather than resolved

1. **`uptime_seconds` reports ~325,000s (3.8 days) on a process deployed 20
   minutes earlier**, while reading 518s immediately after the deploy. The value
   is self-inconsistent, so anything derived from it — including the egress 30d
   projection — cannot be trusted. Worth an investigation; a health field that
   lies is the category this repo has been bitten by repeatedly.
2. **Egress projects ~3.6 GB/30d** against the ~105 MB/30d recorded on
   2026-08-22. The raw counters are 27,412 calls / 449.8 MB (supabase 18,369,
   graph 8,870, llm 172, gmail 1). Since the projection divides by the suspect
   uptime above, the discrepancy may be an artefact of anomaly 1 rather than a
   real regression — but it needs a clean measurement before being dismissed.

## Outcome
- **Verdict:** deployed clean; the crash it targets has not recurred, but the
  8.5-hour idle cycle needed to prove that has not elapsed yet (see T+24h).
- **What broke:** nothing attributable to this deploy. Pre-existing and still
  open: `unclaimable_emails: 27` — pending rows that violate the claimable
  invariant, so they neither process nor retry. Not dead-lettered. Most likely
  wreckage from the crashing attachment loop burning attempts. Requires
  `scripts/repair_review_queue_integrity.py` with an explicit manifest and a
  confirmed user; not touched tonight.
- **Which gate should have caught it:** No local gate can. This needs hours of a
  genuinely idle queue; a laptop run and a diff are both blind to it. It was
  found by reading production logs, which is the case for the T+24h row being
  mandatory rather than nice-to-have.
- **Gate change made as a result:** Regression tests now straddle the float
  limit (1025 / 10_000 / 1_000_000) instead of stopping at 100.
