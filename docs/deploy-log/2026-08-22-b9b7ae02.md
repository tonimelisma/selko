# 2026-08-22 — b9b7ae02

Recorded retroactively, same day, from the cutover session. Windows marked
"not recorded" were genuinely not captured; they are left empty rather than
back-filled from memory.

## Shipped
- **Code sha:** b9b7ae02 (plus follow-up #353)
- **Schema version:** 20260831000001
- **Migrations applied:** 12 in the ordered cutover, then 20260831000001
- **Config / env changed:** none
- **Risk dimensions touched:** production-data, config, observability, logic

## Pre-deploy evidence
- **Verification run id:** n/a — predates the lane runner
- **Prod lanes green:** backend Tier 1 green; no aggregate gate existed
- **Rehearsal:** `rehearse_cutover.py --faithful` predicted 82 calendar work
  items; production matched exactly.
- **Affected table row counts:** captured for the cloned tables only

## Observation
| Window | /health | ingestion | egress | RSS | dead letters | task restarts |
|---|---|---|---|---|---|---|
| T+0 | ok | ok | — | — | 0 | 0 |
| T+15m | ok | ok | 105 MB/30d projected (from 942 MB) | stable | 0 | 0 (was 16) |
| T+24h | not recorded | not recorded | not recorded | not recorded | not recorded | not recorded |

## Outcome
- **Verdict:** clean, after one aborted attempt
- **What broke:** the cutover aborted at migration 5 of 12.
  `event_repair_audit_action_check` was violated by a single production row
  holding the pre-rename action value. Fixed forward in the same session.
  Separately, a live auto-apply exposure was closed by #353 with
  `auto_applied: 0` — found before it caused damage.
- **Which gate should have caught it:** the faithful rehearsal. It did not,
  because `event_repair_audit` was absent from the clone list, so the
  constraint was never exercised against the one row that violated it.
- **Gate change made as a result:** #354 — the rehearsal now *derives* the
  tables it must clone from the pending migrations (`ADD CONSTRAINT`,
  `SET NOT NULL`, `UPDATE`, `DELETE`) instead of relying on a hand-maintained
  list. A hand-maintained list of what to check is itself a thing that can be
  wrong, and was.

## Verified improvements
- anon-executable SECURITY DEFINER functions: **45 → 0**
- background task restarts: **16 → 0**
- egress: **942 MB → ~105 MB / 30d projected**
- worker transport: asyncpg over the session pooler
- zero dead letters and zero incidents attributable to the cutover
